language: python

sudo: false
cache: pip

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - pypy
  - pypy3

install:
  - pip install coveralls
  - pip install -e .\[all\]

script:
  - pytest --cov click_plugins --cov-report term-missing

after_success:
  - coveralls
